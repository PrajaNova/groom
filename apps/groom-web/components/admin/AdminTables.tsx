"use client";

// Legacy file stub — replaced to avoid stale references. The real admin tables are
// implemented as separate per-page components under the same folder.

export default function LegacyAdminTablesStub() {
  return null;
}
