{
  "name": "groom",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "force": "pnpx codemod pnpm/catalog",
    "dev:web": "pnpm --filter groom-web dev",
    "dev:server": "pnpm --filter groom-ms dev",
    "build": "pnpm -r build",
    "start": "pnpm -r start",
    "test": "pnpm -r test",
    "lint": "biome check .",
    "format": "biome format --write .",
    "db:push-force": "pnpm -r db:push --accept-data-loss",
    "db:deploy": "bash scripts/db-deploy.sh"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
