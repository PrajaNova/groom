{
  "name": "groom",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "force": "pnpx codemod pnpm/catalog",
    "dev:web": "pnpm --filter groom-web dev",
    "dev:backend": "pnpm --filter groom-backend dev",
    "build": "pnpm -r build",
    "start": "pnpm -r start",
    "test": "pnpm -r test",
    "lint": "biome check .",
    "format": "biome format --write .",
    "db:push-force": "pnpm -r db:push --accept-data-loss",
    "db:deploy": "bash scripts/db-deploy.sh"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "@types/node": "catalog:",
    "concurrently": "catalog:",
    "dotenv": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:"
  }
}
